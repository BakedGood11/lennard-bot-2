# Lennard(o) bot
This project is meant to be a playground as I learn more about SQL and Python; as such, most of the current (21/07/'25) code is a result of repetitive prompting. Expect changes as I think of added modules and work on fine-tuning the initial stabl ~vibe-code~. 

# Handlers
Contains module in charge of handling calls

# LLM
Holds all active "feature" modules for the bot.

### Scheduler
Module in charge of sending "inspirational" messages.

# .env File
To use the bot, create a (.env) file with the following;
- OPENAI_API_KEY
- BOT_TOKEN
- BRAVE_API_KEY
- DB_HOST
- DB_USER
- DB_PASS
- TARGET_CHAT_ID